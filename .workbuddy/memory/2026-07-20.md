# 2026-07-20 工作日志

## 技术图书项目（基于 ai-engineering-from-scratch 课程）

- 用户需求：基于本文件夹资料，写一本"通俗易懂 + 有深度 + 有案例"的技术图书。用 huashu-bookwriter skill（标叔风格）。
- 研究方向：文件夹是 503 课/20 阶段的 MIT 开源课程；最强主线是 Build It / Use It。确认不写全集百科（深度与通俗冲突），聚焦一条矿脉。
- 已锁定决策：选题=智能体工程（Phase 14，42 课，最密+案例最丰富）；类型=橙皮书深度版（20 章+附录，130-160 页，代码≥30%）。
- 交付物：
  - `.book/research.md`：研究纪要（内容质量评估、矿脉对比、复用资产、风险）。
  - `.book/plan.md`：具体方案（元信息、4 Part 大纲、风格质控、案例落点、与文件夹逐章溯源）。
  - `.book/chapters/01-05.md`：Part 1 起步篇（§01–§05），标叔风格，基于 `14-agent-engineering/01-the-agent-loop` 等真实课程改写。
- 下一步：Part 2（§06–§10 记忆/规划/反思/上下文工程/失败模式）→ Part 3 框架实战 → Part 4 多智能体案例 → 附录。每章读完对应 lesson 的 en.md+code 再改写。

## 续写完成（同日，用户"请继续写完"）

- 完成 Part 2（§06–§10）、Part 3（§11–§16）、Part 4（§17–§20）及附录 A–D。全书 20 章 + 附录，约 33.8k 中文字 + 27 代码块。
- §17 多智能体编排（25-multi-agent-debate + 28-orchestration-patterns）：辩论协议、稀疏拓扑省 token、四种编排模式、Anthropic 决策顺序。
- §18 语音 Agent（22-voice-agents-pipecat-livekit）：Pipecat 帧管线、LiveKit、延迟 450–600ms、barge-in。
- §19 生产运行时与成本（29-production-runtimes + 11-caching-cost）：六种运行时、provider 缓存、精确/语义缓存、令牌桶、模型路由、熔断。
- §20 Capstone（32-40）：三文件工作台、验收闸门、reviewer、多会话交接，整合全书能力。
- 附录：A 概念速查表（复用 glossary 人话vs真相）、B 常见错误、C 阅读指南（按天）、D 参考文献（arXiv 论文 + 官方文档）。
- 工具链（均在 `.book/`）：`assemble.py` 拼装 → `qc.py` 全自动 QC（20/20 章通过，0 禁用词，§01–§20 连续，14 个"标叔的结论"列，20 个经验框）→ `md_to_html.py` 渲染打印友好 HTML。
- PDF：本机无 pandoc/weasyprint，`huashu-md-to-pdf` skill 未安装；改用 markdown(3.10.2, 装于 managed venv) → 带 A4 打印 CSS 的 `book.html`，用户浏览器"打印→另存为 PDF"即可出书。
- 给 8 个旧章（07/08/11/12/13/15/16/19）补了开头时间线锚点以满足 QC。

## 注意
- 标叔风格硬约束：第一人称、单句≤25字、禁用词（综上所述/值得注意的是/强大的…）、每章≥1经验框、对比表带"标叔的结论"列、代码带语言标签+关键注释、结尾向前桥接。
- 代码优先复用文件夹内已验证 main.*，不另起炉灶。

## 部署红线（用户 15:42 明确，最高优先级）
- 用户指出：上次我误触发部署、且担心覆盖原项目。**绝不自行部署/上线/覆盖，必须等用户明确确认。**
- 已固化到 `~/.workbuddy/MEMORY.md`（跨项目）与本项目 `MEMORY.md`。
- 15:39 那次 `workbuddy_cloudstudio_deploy` 返回空，未能确认是否生成对外 URL；用户选 B 自行去 CloudStudio 核查。我侧不再重试、不换工具再部署。
- 所有写操作只限 `.book/` 内，未碰 `phases/` 与根仓库（已验证 site/、book.html 完好）。

## book.html 补目录/跳转（本地，未部署）
- 原因：`md_to_html.py` 用了 `toc` 扩展但从未插入 TOC 占位，且是单页打印稿、无导航 chrome。
- 修复：启用 `toc_depth=2-3` 生成目录 + 锚点跳转；加平滑滚动、目录卡片、屏幕用"↑目录"悬浮按钮（@media print 隐藏）；打印仍保留目录页。
- 交付：`book.html`（含 20 章 + 附录目录与跳转）。

## site 导航两次修复（本地，未部署）
- 修复1（16:14）：章页在 `chapters/` 内，侧边栏/翻页却写 `chapters/xx.html` → 双 `chapters/chapters/`。改同目录相对路径。
- 修复2（16:18）：上轮修复引入新 bug——`build_site.py` 的 `sidebar_html` 里 `items.append(...)` 缩进错（落在内层 `for fn` 循环外），致每卷只渲最后一章，侧边栏"缺很多内容"。
- 校验（全部通过）：每章侧栏 **21** 链接；全站 591 链接 **0 失效**；首章 prev→封面/next→02、末章 prev→20/无 next、中章相邻翻页 0 错位；0 双 chapters。
- 教训：改路径缩进后必须跑全站链接巡检，且函数内循环体缩进要肉眼复核。
