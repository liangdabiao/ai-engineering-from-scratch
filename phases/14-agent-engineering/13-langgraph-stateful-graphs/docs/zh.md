# LangGraph：有状态图与持久化执行

> LangGraph 是 2026 年低级有状态编排的参考。智能体(Agent)是一个状态机(state machine)；节点(nodes)是函数(functions)；边(edges)是转换(transitions)；状态(state)是不可变(immutable)的，并在每一步后进行检查点(checkpointed)。从任何故障处精确恢复(resume)到中断的地方。

**类型：** 学习+构建
**语言：** Python (标准库)
**前提条件：** 阶段14·01 (智能体循环), 阶段14·12 (工作流模式)
**时间：** 约75分钟

## 学习目标

- 描述LangGraph的核心模型：具有不可变状态的状态机、函数节点、条件边和步后检查点。
- 列举文档强调的四个能力：持久执行、流式处理、人在回路、全面记忆。
- 解释LangGraph支持的三种编排拓扑：监督者、点对点（群集）、分层（嵌套子图）。
- 使用标准库实现一个具有不可变状态、条件边和检查点/恢复周期的状态图。

## 问题

智能体和工作流共有一个问题：当一个40步的运行在第38步失败时，你希望从第38步恢复，而不是重新开始。二等状态模型(Second-class state models)让操作者在一个假设全新运行的库周围拼凑重试逻辑。

LangGraph的设计答案：状态是一等类型对象(first-class typed object)，突变(mutations)是显式的，检查点在每个节点后持久化。恢复是一个`load_state(session_id)`调用。

## 核心概念

### 图

图由以下定义：

- **状态类型。** 每个节点读取和修改的类型化字典（或Pydantic模型）。
- **节点。** 纯函数(Pure functions)`(state) -> state_update`。返回后更新被合并到状态中。
- **边。** 节点之间的条件或直接转换。
- **进入和退出。** `(state) -> state_update`和`START`哨兵节点标记边界。

示例：一个具有`classify`、`refund`、`bug`、`sales`、`done`节点的智能体——一个作为图的路由工作流。

### 持久执行

每个节点返回后，运行时序列化状态并将其写入检查点器(checkpointer)（SQLite、Postgres、Redis、自定义）。在第N步失败时，运行时可以`resume(session_id)`并从第N+1步开始，使用精确的状态。

LangGraph文档明确强调了对此重视的生产用户：Klarna、Uber、J.P. Morgan。关键不在于图的形式，而在于图的形式加上检查点使得恢复成本低廉。

### 流式传输

每个节点可以产生部分输出。图将每个节点的增量事件流式传输给调用者，以便UI在图运行时更新。

### 人工介入(Human-in-the-loop)

在节点之间检查和修改状态。实现方式：在关键节点前暂停，将状态呈现给人类，接受修改，恢复。检查点器使这变得容易，因为状态已经序列化。

### 记忆

短期（运行内——状态中的对话历史）和长期（跨运行——通过检查点器持久化加上单独的长时存储器）。LangGraph通过工具与外部记忆系统（Mem0、自定义）集成。

### 三种拓扑

1. **监督者。** 中央路由器LLM分派给专业子智能体。`create_supervisor()`在`langgraph-supervisor`中（尽管LangChain团队在2026年建议直接通过工具调用来实现，以获得更好的上下文控制）。
2. **群集/点对点。** 智能体通过共享的工具表面直接交接。没有中央路由器。
3. **分层。** 监督者管理子监督者，实现为嵌套子图。

### 这种模式出错的地方

- **检查点太小。** 仅检查对话轮次会使工具状态和记忆写入不可恢复。必须序列化完整状态。
- **非确定性节点。** 恢复假设节点输入产生相同的状态更新。随机种子、实时时钟、外部API必须被捕获。
- **过度使用条件边。** 每条边都是条件边的图是一个无法推理的状态机。首选带有偶尔分支的线性链。

## 动手构建

`code/main.py`实现了一个标准库有状态图：

- `State` — 一个带有`messages`、`step`、`route`、`output`、`human_approval`的类型化字典。
- `State` — 可调用对象，接收状态并返回更新字典。
- `State` — 节点+边+条件边+运行+恢复。
- `State`（内存模拟）— 在每个节点后序列化状态；`messages`恢复。
- 一个演示图：分类 -> 分支（退款/缺陷/销售）-> 人工门控 -> 发送。

运行它：

```
python3 code/main.py
```

跟踪显示第一次运行在人工门控处失败，持久化，然后恢复产生最终输出。

## 使用它

- **LangGraph** — 参考实现，生产就绪。使用`create_react_agent`、`create_supervisor`或构建自己的图。
- **AutoGen v0.4**（第14课）— 用于高并发场景的参与者模型替代方案。
- **Claude Agent SDK**（第17课）— 具有内置会话存储的管理框架。
- **Custom** — 当你需要对状态形状或检查点器后端进行精确控制时。

## 发布

`outputs/skill-state-graph.md`在任何目标运行时中生成LangGraph形状的状态图，并内置检查点和恢复。

## 练习

1. 当分类置信度低于阈值时，添加一条从`classify`到`end`的条件边。在人工手动设置`route`后恢复运行。
2. 将类似SQLite的模拟替换为真实的SQLite检查点器。测量每步序列化开销。
3. 实现并行边：两个节点并发运行，由自定义归约器(reducer)合并。不可变状态在这里带来了什么？
4. 阅读`classify`参考。将玩具移植到`end`。比较跟踪形状。
5. 添加流式：每个节点运行时产生部分状态。打印到达的增量。

## 关键术语

|  术语  |  人们的说法  |  实际含义  |
|------|----------------|------------------------|
| 状态图  |  "智能体即状态机"  |  类型化状态+节点+边+归约器 |
| 检查点器  |  "持久化后端"  |  在每个节点后序列化状态；启用恢复 |
| 归约器  |  "状态合并器"  |  将当前状态与节点更新组合的函数 |
| 条件边  |  "分支"  |  由状态函数选择的边 |
| 子图  |  "嵌套图"  |  用作另一个图内部节点的图 |
|  持久执行 | "从失败恢复" | 在最后一个成功的节点处以精确状态重新启动  |
|  监督者 | "路由大语言模型" | 专分子智能体的中央调度器  |
|  群体 | "点对点智能体" | 智能体通过共享工具交接；无中央路由器  |

## 延伸阅读

- [LangGraph overview](https://docs.langchain.com/oss/python/langgraph/overview) — 参考文档
- [LangGraph overview](https://docs.langchain.com/oss/python/langgraph/overview) — 监督者模式API
- [LangGraph overview](https://docs.langchain.com/oss/python/langgraph/overview) — 参与者模型替代方案
- [LangGraph overview](https://docs.langchain.com/oss/python/langgraph/overview) — 会话存储和子智能体
